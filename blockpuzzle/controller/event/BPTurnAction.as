import blockpuzzle.model.data.BPDirection;import blockpuzzle.model.game.BPActor;import blockpuzzle.controller.event.*;class blockpuzzle.controller.event.BPTurnAction extends BPBehavior {		var direction:BPDirection;	var oldFrame:Number;		function BPTurnAction(actor:BPActor, direction:BPDirection) {		super(actor);		setKey("Turn");				this.direction = direction;	}		function act() {		// var compassDir		// 		// oldFrame = actor().getMovieClip()._currentFrame		// 		// if (direction == BPDirection.North){		// 	compassDir = 'Back'		// }		// else if (direction == BPDirection.South){		// 	compassDir = 'Front'		// }		// else if (direction == BPDirection.East){		// 	compassDir = 'Right'		// }		// else if (direction == BPDirection.West){		// 	compassDir = 'Left'		// }		// actor().setFrame(compassDir + '_0')			}		function undo() {		//return character to previous direction		//package library.controller.event;        actor.setFrame(oldFrame);	}	//function toString():String {	//	return "Turn " + actor() + " to direction " + direction;	//}}